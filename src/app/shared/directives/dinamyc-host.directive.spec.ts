import { DinamycHostDirective } from './dinamyc-host.directive';

describe('DinamycHostDirective', () => {
  it('should create an instance', () => {
    const directive = new DinamycHostDirective();
    expect(directive).toBeTruthy();
  });
});
